import {FC} from 'react';
import {CallType} from '../../../../api/globalApi';

export const Table: FC<TablePropsType> = ({data}) => {
    return <div>

    </div>
}

//type
type TablePropsType = {
    data: CallType[]
}